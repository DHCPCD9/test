name: Update osu!lazer winget package

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'version number'
        required: true
        

jobs:
  wingetcreate-update:
    runs-on: windows-latest

    steps:
      - name: Download & Run wingetcreate update
        shell: cmd
        run: |
          curl https://aka.ms/wingetcreate/latest/self-contained -L --output wingetcreate.exe
          wingetcreate.exe update Peppy.Osu! -v ${{ github.event.inputs.version }} -t ${{ secrets.GH_TOKEN }} -s
